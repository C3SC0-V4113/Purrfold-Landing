# Apply Progress: Phase 2 Design Shell Navigation

## Status

- Slice: 2 of 2
- Delivery: chained PR slice (`stacked-to-main`)
- Mode: Strict TDD
- Scope status: slice 1 and slice 2 implementation complete; remediation follow-up fixed the post-Playwright typecheck/check blocker by regenerating route types from a clean state, and formal `sdd-verify` remains a separate phase

## Completed Tasks

- [x] 1.1 Create `tests/unit/theme-switcher.test.tsx` for dropdown labels, theme selection, and route-stable behavior.
- [x] 1.2 Create `tests/unit/language-switcher.test.tsx` for locale options and `router.replace` path/query/hash preservation.
- [x] 1.3 Create `tests/e2e/mobile-navigation.spec.ts` for mobile sheet open/close and GitHub/shadcn/Next.js href placement.
- [x] 2.1 Add `components/ui/navigation-menu.tsx` from shadcn base-rhea and verify the generated API matches Next.js `Link` composition.
- [x] 2.2 Refactor `components/base-navigation.tsx` desktop nav to `NavigationMenu`, keep `Sheet` mobile nav, and reduce navbar externals to GitHub only.
- [x] 2.3 Update `app/[locale]/page.tsx` and `messages/en.json`, `messages/es.json` with a localized home foundations section for shadcn and Next.js links.
- [x] 2.4 Add minimal shared motion tokens and reduced-motion guards in `app/globals.css` only.
- [x] 3.1 Reconcile nav labels, icon affordances, and safe external-link attributes across desktop/mobile/header/home states in `components/base-navigation.tsx`.
- [x] 3.2 Review client boundaries and localized routing assumptions so nav, theme, and language controls stay compatible with Next.js 16 patterns.
- [x] 4.1 Create `docs/adr/0002-design-system-boundaries.md` for shadcn/base-rhea boundaries, Base UI `render`, lucide usage, motion limits, and MCP importance.
- [x] 4.2 Update `docs/adr/0001-i18n-and-routing.md`, `AGENTS.md`, and `README.md` to align ADR status and document repo-level `opencode.json` shadcn MCP enablement.
- [x] 5.1 Run final verification commands, using targeted Chromium mobile-navigation E2E plus `npm run check` instead of a separate full `npm run test:e2e` pass.
- [x] 5.2 Capture verification notes against spec scenarios before apply complete.

## TDD Cycle Evidence

| Task | Test File                                                                                                                    | Layer             | Safety Net                                                                                                                        | RED                                                                                                                                                                          | GREEN                                                                                                                                                                                                                                       | TRIANGULATE                                                            | REFACTOR                                                                                                      |
| ---- | ---------------------------------------------------------------------------------------------------------------------------- | ----------------- | --------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------- |
| 1.1  | `tests/unit/theme-switcher.test.tsx`                                                                                         | Unit              | ✅ `npm run test -- tests/unit/theme-switcher.test.tsx`                                                                           | ✅ Existing resumed regression spec covered theme behavior before implementation resumed                                                                                     | ✅ `npm run test -- tests/unit/theme-switcher.test.tsx`; later passed again in `npm run test`                                                                                                                                               | ✅ Dropdown labels + route-stable theme selection                      | ✅ Import order and formatting aligned with repo rules                                                        |
| 1.2  | `tests/unit/language-switcher.test.tsx`                                                                                      | Unit              | ✅ `npm run test -- tests/unit/language-switcher.test.tsx`                                                                        | ✅ Existing resumed regression spec covered locale preservation before implementation resumed                                                                                | ✅ `npm run test -- tests/unit/language-switcher.test.tsx`; later passed again in `npm run test`                                                                                                                                            | ✅ Locale labels + preserved path/query/hash                           | ✅ Test kept after lint/format cleanup                                                                        |
| 1.3  | `tests/e2e/mobile-navigation.spec.ts`                                                                                        | E2E               | N/A (new file in slice 1)                                                                                                         | ✅ Existing resumed spec defined the mobile and external-link contract                                                                                                       | ✅ `CI=1 npx playwright test tests/e2e/mobile-navigation.spec.ts --project=chromium --workers=1`                                                                                                                                            | ✅ Open/close behavior + external href placement                       | ✅ Focused Chromium coverage kept as the durable browser proof                                                |
| 2.1  | `tests/unit/localized-pages.test.tsx`                                                                                        | Integration       | ✅ `npm run test -- tests/unit/localized-pages.test.tsx`                                                                          | ✅ Localized page rendering failed before the partial desktop nav integration was corrected                                                                                  | ✅ Desktop header renders with the generated shadcn wrapper and localized page tests pass                                                                                                                                                   | ✅ English + Spanish localized rendering paths                         | ✅ Generated component normalized to repo formatting and client-component conventions                         |
| 2.2  | `tests/unit/localized-pages.test.tsx`, `tests/e2e/mobile-navigation.spec.ts`                                                 | Integration + E2E | ✅ `npm run test -- tests/unit/localized-pages.test.tsx tests/unit/theme-switcher.test.tsx tests/unit/language-switcher.test.tsx` | ✅ Targeted unit tests and Playwright failed before the navbar integration and GitHub icon fix                                                                               | ✅ Desktop nav uses `NavigationMenu`, mobile `Sheet` remains intact, and GitHub is the only navbar external link                                                                                                                            | ✅ Desktop localized links + mobile external link behavior             | ✅ Import order, icon choice, and nav styling cleaned after the root-cause fix                                |
| 2.3  | `tests/unit/localized-pages.test.tsx`                                                                                        | Integration       | ✅ `npm run test -- tests/unit/localized-pages.test.tsx`                                                                          | ✅ Home/nav split initially failed localized page rendering                                                                                                                  | ✅ Localized foundations links render correctly on `/en` and `/es`                                                                                                                                                                          | ✅ Header exclusion + main-content inclusion for shadcn/Next.js links  | ✅ `PageShell` usage refactored to nested children so lint passes cleanly                                     |
| 2.4  | `tests/unit/design-shell-navigation-artifacts.test.ts`                                                                       | Unit              | ✅ `npm run test -- tests/unit/localized-pages.test.tsx tests/unit/theme-switcher.test.tsx tests/unit/language-switcher.test.tsx` | ✅ `tests/unit/design-shell-navigation-artifacts.test.ts` was written first and failed because `app/globals.css` lacked shared shell motion tokens and reduced-motion guards | ✅ `npm run test -- tests/unit/design-shell-navigation-artifacts.test.ts`                                                                                                                                                                   | ✅ Shared-token assertions + reduced-motion override assertions        | ✅ Tokens kept to two CSS-only shell animations with no library expansion                                     |
| 3.1  | `tests/unit/localized-pages.test.tsx`, `tests/e2e/mobile-navigation.spec.ts`                                                 | Integration + E2E | ✅ `npm run test -- tests/unit/localized-pages.test.tsx tests/unit/theme-switcher.test.tsx tests/unit/language-switcher.test.tsx` | ✅ Same slice-1 RED as 2.2/2.3: partial nav state failed localized rendering before reconciliation                                                                           | ✅ Desktop/mobile/header/home states now share GitHub-only navbar external behavior with safe `target`/`rel` attributes                                                                                                                     | ✅ Header behavior + main-content foundations behavior                 | ✅ External-link affordances simplified to lucide `GitBranchIcon` + `ExternalLinkIcon`                        |
| 3.2  | `tests/unit/localized-pages.test.tsx`, `tests/unit/language-switcher.test.tsx`                                               | Integration       | ✅ `npm run test -- tests/unit/localized-pages.test.tsx tests/unit/language-switcher.test.tsx`                                    | ✅ Same slice-1 RED as 2.1-2.3: routing/client boundaries were inconsistent until reconciled                                                                                 | ✅ `npm run lint`, `npm run typecheck`, `npm run test`, and targeted routing/browser checks passed in slice 1                                                                                                                               | ✅ Deep localized page rendering + locale-switch preservation          | ✅ Next.js 16 route usage stayed server-safe on pages and client-only in control islands                      |
| 4.1  | `tests/unit/design-shell-navigation-artifacts.test.ts`                                                                       | Unit              | N/A (new file)                                                                                                                    | ✅ `tests/unit/design-shell-navigation-artifacts.test.ts` failed because ADR 0002 did not exist                                                                              | ✅ `npm run test -- tests/unit/design-shell-navigation-artifacts.test.ts`                                                                                                                                                                   | ✅ Title/status assertions + boundary-content assertions               | ✅ ADR kept lightweight but durable around shadcn/base-rhea, Base UI `render`, lucide, motion limits, and MCP |
| 4.2  | `tests/unit/design-shell-navigation-artifacts.test.ts`                                                                       | Unit              | N/A (docs alignment task)                                                                                                         | ✅ The same RED run failed because ADR 0001 was still Proposed and repo docs did not mention repo-level MCP enablement                                                       | ✅ `npm run test -- tests/unit/design-shell-navigation-artifacts.test.ts`                                                                                                                                                                   | ✅ ADR status assertions + AGENTS/README MCP assertions                | ✅ Repo docs now use one shared repo-level `opencode.json` explanation                                        |
| 5.1  | `tests/unit/design-shell-navigation-artifacts.test.ts`, `tests/e2e/mobile-navigation.spec.ts`                                | Verification      | N/A (verification task)                                                                                                           | ✅ Verification plan came from the task contract and user override before command execution                                                                                  | ✅ `npm run lint`; `npm run typecheck`; `npm run format:check`; `npm run test`; targeted Chromium E2E with `CI=1 npx playwright test tests/e2e/mobile-navigation.spec.ts --project=chromium --workers=1`; `npm run doctor`; `npm run check` | ➖ Verification task; no additional behavior branch beyond command set | ✅ Added `.prettierignore` entry for generated `messages/*.d.json.ts` so the format gate stays stable         |
| 5.2  | `tests/unit/design-shell-navigation-artifacts.test.ts`, `openspec/changes/phase-2-design-shell-navigation/apply-progress.md` | Verification      | N/A (artifact task)                                                                                                               | ✅ Spec scenarios were enumerated before final note capture                                                                                                                  | ✅ Final verification notes are recorded below against each requirement                                                                                                                                                                     | ✅ Notes cover motion, docs, controls, nav, and verification gates     | ➖ None needed                                                                                                |

## Verification Notes

### Command Results

- Port 3100 check: `Get-NetTCPConnection -LocalPort 3100 -State Listen` returned no listener; no stale project-owned server needed cleanup.
- `npm run lint` ✅
- `npm run typecheck` ✅ after deleting stale `.next/dev/types` artifacts and rerunning `next typegen`
- `npm run format:check` ✅
- `npm run test` ✅
- `CI=1 npx playwright test tests/e2e/mobile-navigation.spec.ts --project=chromium --workers=1` ✅
- `npm run doctor` ✅ (reported maintainability warnings but did not fail)
- `npm run check` ✅ after the typecheck script was hardened to clean stale dev route types before regeneration

### Spec Scenario Coverage

- **Desktop primary navigation menu**: preserved from slice 1 and re-verified through `tests/unit/localized-pages.test.tsx` plus `npm run test`; localized desktop links and active-state contract remain covered.
- **Mobile sheet navigation accessibility**: `tests/e2e/mobile-navigation.spec.ts` passed in Chromium, proving open/close behavior and accessible controls still work after slice 2.
- **External link placement and safety**: slice-1 browser/unit coverage remains green; GitHub stays the only navbar external link while shadcn/Next.js stay on the home foundations section.
- **Home foundations links**: `tests/unit/localized-pages.test.tsx` remains green and `tests/unit/design-shell-navigation-artifacts.test.ts` confirms supporting docs/ADR alignment.
- **Theme and language controls preserve context**: `tests/unit/theme-switcher.test.tsx` and `tests/unit/language-switcher.test.tsx` passed inside `npm run test`.
- **Minimal motion tokens respect reduced motion**: `tests/unit/design-shell-navigation-artifacts.test.ts` verifies shared shell tokens plus reduced-motion overrides in `app/globals.css`.
- **Documentation and verification stay aligned**: ADR 0002 exists, ADR 0001 is Accepted, AGENTS/README document repo-level `opencode.json` MCP enablement, and the required verification commands passed.

## Notes

- 2026-06-15 remediation follow-up: the real blocker was stale `.next/dev/types` output left behind after Playwright's `next dev` run. Because `tsconfig.json` includes `.next/dev/types/**/*.ts`, malformed dev route helpers can break `tsc` even though `next typegen` regenerates `next-env.d.ts` back to `.next/types/routes.d.ts`. The durable fix is targeted cleanup of `.next/dev/types` before `next typegen`, which keeps `npm run typecheck` and `npm run check` reproducible without deleting all of `.next/dev` or removing `next-env.d.ts`.
- 2026-06-15 verifier remediation: added a focused localized-pages assertion that every GitHub navbar external action keeps `target="_blank"` plus `rel="noreferrer noopener"`, closing the last formal spec coverage gap without changing navigation implementation.
- 2026-06-15 remediation: replaced invalid deep-route `PageProps<'/[locale]/...'>` usage with a shared async locale-props type so Next.js 16 type-checking no longer rejects nested localized pages, and expanded runtime coverage for desktop active-route `aria-current` plus mobile sheet accessible names while re-validating `/en/install`.
- The task list originally named `npm run test:e2e`; it now matches the explicit apply instruction to avoid a separate full E2E pass and instead use the focused Chromium mobile-navigation spec plus `npm run check` required by the design verification gate.
- `.prettierignore` now excludes generated `messages/*.d.json.ts` files because the next-intl generator rewrites them in a style that drifts from repo Prettier output during verification.
